input {
  stdin { }
}

filter {
  # basic example — add fields or grok as needed
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "logstash-test-%{+YYYY.MM.dd}"
  }

  stdout { codec => rubydebug }
}
